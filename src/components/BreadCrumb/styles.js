import styled from "styled-components";

export const Container = styled.div`
  width: 100%;
  display: flex;
  padding: 1.6rem 4rem;
  font-size: 1.2rem;
  color: var(--color-complement1);

  p {
    font-weight: bold;
  }

  span {
    margin: 0 0.8rem;
  }
`;
